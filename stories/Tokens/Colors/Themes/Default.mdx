import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

import { Colors } from '~/assets/scripts/tokens.ts'

import { capitalize } from '~/assets/scripts/utils.ts';

export const themeName = 'default';

<Meta title="Tokens/Colors/Themes/Default" />

<h1>{capitalize(themeName)}</h1>

<ColorPalette>
{Object.entries(Colors.theme[themeName]).map((category, i) => {
  return (
    <ColorItem
      key={category[0]}
      title={capitalize(category[0])}
      subtitle={`--color-${category[0]}-$swatch`}
      colors={{...category[1]}}
    />
  )
})}
</ColorPalette>

## Palette

<ColorPalette>
{Object.entries(Colors.palette[themeName]).map((color, i) => {
  return (
    <ColorItem
      key={color[0]}
      title={capitalize(color[0])}
      subtitle={`--color-palette-${color[0]}`}
      colors={{ [color[0]]: color[1] }}
    />
  )})
}
</ColorPalette>